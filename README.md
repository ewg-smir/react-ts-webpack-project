 🚀 Инструкция по запуску проекта

Это руководство поможет вам запустить проект для разработки и тестирования.

 📋 Требования

Для запуска проекта на вашем компьютере должно быть установлено следующее:

  * **Node.js** (рекомендуется версия 18 или выше).
  * **npm** (менеджер пакетов, поставляется вместе с Node.js).

 ⚙️ Установка зависимостей

1.  Клонируйте репозиторий (если вы еще этого не сделали):

    ```bash
    git clone [ссылка на ваш репозиторий]
    cd [имя-папки-проекта]
    ```

2.  Установите все необходимые зависимости проекта:

    ```bash
    npm install
    ```

    или, если вы используете Yarn:

    ```bash
    yarn install
    ```

 ▶️ Запуск в режиме разработки

Для запуска локального сервера разработки с автоматической перезагрузкой (Hot Module Replacement):

```bash
npm run start
```

или, если у вас в `package.json` другая команда для разработки (например, `dev`):

```bash
npm run dev
```

После запуска сервер будет доступен по адресу: **`http://localhost:8080`** (порт может отличаться в зависимости от конфигурации Webpack).

 📦 Сборка проекта (Production)

Для создания оптимизированной статической сборки проекта (файлы готовы к деплою на сервер):

```bash
npm run build
```

Все готовые файлы для продакшена будут находиться в папке **`./dist`**.

-----

 📝 Дополнительные команды (Опционально)

  * **Запуск линтера (ESLint):**
    ```bash
    npm run lint
    ```
  * **Запуск форматирования (Prettier):**
    ```bash
    npm run format
    ```
